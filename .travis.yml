language: python

python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"

install:
    - "python setup.py install"
    - "pip install coverage"
    - "pip install coveralls"
    - "pip install pytest"

script:
#    - "coverage run --source=pysqlbuilder setup.py test"
    - "pytest"
after_success:
    coveralls